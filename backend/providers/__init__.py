"""Data Provider SDK and connectors (Phase 2, Module 1–2).

Ports (`providers.sdk`) are framework-free and provider-agnostic; the
domain never sees provider-specific code. Connectors
(`providers.connectors`) are pure adapters — no business logic —
selected at runtime through configuration (ProviderRegistry).
"""
