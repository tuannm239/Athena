"""Backtest Engine bounded context (SPEC-09, ALG-013).

Validates decision logic against historical data before production use;
no investment rule may be promoted without successful backtesting.
Deterministic, bias-guarded simulation (ADR-0015).
"""
