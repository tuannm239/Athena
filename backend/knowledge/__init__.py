"""Knowledge Graph bounded context (RFC-0019).

Canonical relationship model across companies, industries, macro
indicators, events and decisions. Storage-independent (ADR-0007);
supports explainable, deterministic reasoning.
"""
